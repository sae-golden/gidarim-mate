# 시험관메이트 - 병원 검색 기능 구현

## 요구사항
설정 > 병원 정보에서 난임 전문 병원을 검색하고 선택할 수 있는 기능 구현

## API 정보
- **API명**: 건강보험심사평가원 병원정보서비스
- **인증키**: `9452f9e3e312293e084b9a4b34ca590f2acf7f4342eae5fafc2b02920d470a15`
- **Base URL**: `http://apis.data.go.kr/B551182/hospInfoServicev2`

### 엔드포인트
```
GET /getHospBasisList  - 병원 기본 정보 조회
```

### 주요 파라미터
| 파라미터 | 설명 | 예시 |
|----------|------|------|
| serviceKey | 인증키 | (위 키 사용) |
| pageNo | 페이지 번호 | 1 |
| numOfRows | 한 페이지 결과 수 | 20 |
| sidoCd | 시도 코드 | 110000 (서울) |
| sgguCd | 시군구 코드 | (선택) |
| emdongNm | 읍면동 이름 | (선택) |
| yadmNm | 요양기관명 (병원명) | 차여성 |
| dgsbjtCd | 진료과목 코드 | 11 (산부인과) |

### 응답 데이터 (XML/JSON)
```json
{
  "yadmNm": "차여성의학연구소서울역센터",  // 병원명
  "addr": "서울특별시 용산구...",           // 주소
  "telno": "02-2002-0300",                 // 전화번호
  "XPos": "126.97",                        // 경도
  "YPos": "37.55",                         // 위도
  "clCdNm": "의원",                        // 종별명
  "sidoCdNm": "서울",                      // 시도명
  "sgguCdNm": "용산구"                     // 시군구명
}
```

## UI 구현

### 1. 병원 정보 메인 화면
```
┌─────────────────────────────────────────┐
│ ← 병원 정보                             │
├─────────────────────────────────────────┤
│                                         │
│ 🏥 등록된 병원                          │
│ ┌─────────────────────────────────────┐ │
│ │ 차여성의학연구소 강남                │ │
│ │ 서울 강남구 | 02-3468-3000          │ │
│ │                          [변경하기] │ │
│ └─────────────────────────────────────┘ │
│                                         │
│ 👨‍⚕️ 담당 선생님                         │
│ ┌─────────────────────────────────────┐ │
│ │ [김OO 교수님                      ] │ │
│ └─────────────────────────────────────┘ │
│                                         │
│ 📝 메모                                 │
│ ┌─────────────────────────────────────┐ │
│ │ [3층 생식의학센터                  ] │ │
│ └─────────────────────────────────────┘ │
│                                         │
│           [📞 병원 전화하기]            │
│                                         │
└─────────────────────────────────────────┘
```

### 2. 병원 검색 화면 (변경하기 탭 시)
```
┌─────────────────────────────────────────┐
│ ← 병원 선택                             │
├─────────────────────────────────────────┤
│ ┌─────────────────────────────────────┐ │
│ │ 🔍 병원명으로 검색                  │ │
│ └─────────────────────────────────────┘ │
│                                         │
│ 📍 내 주변 난임 전문 병원               │
│                                         │
│ ┌─────────────────────────────────────┐ │
│ │ 🏥 차여성의학연구소 강남            │ │
│ │    서울 강남구 | 02-3468-3000    >  │ │
│ ├─────────────────────────────────────┤ │
│ │ 🏥 서울마리아병원                   │ │
│ │    서울 서초구 | 02-2250-5500    >  │ │
│ ├─────────────────────────────────────┤ │
│ │ 🏥 미즈메디병원                     │ │
│ │    서울 강서구 | 02-2007-1000    >  │ │
│ ├─────────────────────────────────────┤ │
│ │ 🏥 강남차병원                       │ │
│ │    서울 강남구 | 02-3468-3000    >  │ │
│ └─────────────────────────────────────┘ │
│                                         │
│ 💡 원하는 병원이 없나요?                │
│    [직접 입력하기]                      │
│                                         │
└─────────────────────────────────────────┘
```

### 3. 검색 결과 화면
```
┌─────────────────────────────────────────┐
│ ← 병원 선택                             │
├─────────────────────────────────────────┤
│ ┌─────────────────────────────────────┐ │
│ │ 🔍 차병원                      [X]  │ │
│ └─────────────────────────────────────┘ │
│                                         │
│ 🔎 검색 결과 (12건)                     │
│                                         │
│ ┌─────────────────────────────────────┐ │
│ │ 🏥 차여성의학연구소 강남            │ │
│ │    서울 강남구 논현로 566           │ │
│ │    02-3468-3000                  >  │ │
│ ├─────────────────────────────────────┤ │
│ │ 🏥 차여성의학연구소 서울역          │ │
│ │    서울 용산구 한강대로 416         │ │
│ │    02-2002-0300                  >  │ │
│ ├─────────────────────────────────────┤ │
│ │ 🏥 분당차여성병원                   │ │
│ │    경기 성남시 분당구 야탑로 59     │ │
│ │    031-780-5000                  >  │ │
│ └─────────────────────────────────────┘ │
│                                         │
└─────────────────────────────────────────┘
```

## 구현 로직

### 1. API 호출 서비스
```dart
class HospitalService {
  static const String baseUrl = 'http://apis.data.go.kr/B551182/hospInfoServicev2';
  static const String serviceKey = '9452f9e3e312293e084b9a4b34ca590f2acf7f4342eae5fafc2b02920d470a15';
  
  // 병원 검색
  Future<List<Hospital>> searchHospitals({
    String? keyword,      // 병원명 검색어
    String? sidoCd,       // 시도 코드
    int pageNo = 1,
    int numOfRows = 20,
  }) async {
    final uri = Uri.parse('$baseUrl/getHospBasisList').replace(
      queryParameters: {
        'serviceKey': serviceKey,
        'pageNo': pageNo.toString(),
        'numOfRows': numOfRows.toString(),
        'dgsbjtCd': '11',  // 산부인과
        if (keyword != null) 'yadmNm': keyword,
        if (sidoCd != null) 'sidoCd': sidoCd,
        '_type': 'json',
      },
    );
    
    final response = await http.get(uri);
    // 파싱 로직...
  }
  
  // 현재 위치 기반 검색 (위치 권한 필요)
  Future<List<Hospital>> getNearbyHospitals(double lat, double lng) async {
    // 시도 코드 추출 후 검색
    // 또는 전체 검색 후 거리 계산으로 정렬
  }
}
```

### 2. 병원 모델
```dart
class Hospital {
  final String name;        // yadmNm
  final String address;     // addr
  final String phone;       // telno
  final String? sidoName;   // sidoCdNm
  final String? sgguName;   // sgguCdNm
  final double? latitude;   // YPos
  final double? longitude;  // XPos
  
  // 간략 주소 (시군구만)
  String get shortAddress => '$sidoName $sgguName';
}
```

### 3. 저장 데이터
```dart
class UserHospitalInfo {
  final Hospital? hospital;     // 선택한 병원
  final String? doctorName;     // 담당 선생님
  final String? memo;           // 메모
}
```

## 추가 기능

### 1. 전화 바로걸기
```dart
// url_launcher 패키지 사용
void callHospital(String phoneNumber) {
  launchUrl(Uri.parse('tel:$phoneNumber'));
}
```

### 2. 위치 보기 (지도 연동)
```dart
// 카카오맵 또는 네이버맵 연동
void openMap(Hospital hospital) {
  final url = 'https://map.kakao.com/link/search/${hospital.name}';
  launchUrl(Uri.parse(url));
}
```

### 3. 직접 입력
- API에 없는 병원은 사용자가 직접 입력 가능
- 병원명, 전화번호, 주소 입력 필드 제공

## 시도 코드 (참고)
| 코드 | 시도 |
|------|------|
| 110000 | 서울 |
| 210000 | 부산 |
| 220000 | 인천 |
| 230000 | 대구 |
| 240000 | 광주 |
| 250000 | 대전 |
| 260000 | 울산 |
| 310000 | 경기 |
| 320000 | 강원 |
| 330000 | 충북 |
| 340000 | 충남 |
| 350000 | 전북 |
| 360000 | 전남 |
| 370000 | 경북 |
| 380000 | 경남 |
| 390000 | 제주 |
| 410000 | 세종 |

## 우선순위
1. [P0] 병원 검색 (키워드)
2. [P0] 병원 선택 & 저장
3. [P0] 전화 바로걸기
4. [P1] 내 주변 병원 (위치 기반)
5. [P2] 직접 입력
6. [P2] 지도 연동
