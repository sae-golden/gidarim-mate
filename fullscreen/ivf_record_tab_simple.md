# 시험관메이트 - 기록 탭 개선 (심플 버전)

## 개요
기록 탭의 치료 단계를 심플하게 표시하고, 바텀시트로 간편하게 편집하는 방식

## 핵심 변경사항

### 기존 → 개선
| 기존 | 개선 |
|------|------|
| 0차 채취 (기본값) | **1차 채취** (기본값) |
| 현재 단계만 표시 | **전체 단계 항상 표시** |
| 복잡한 결과 입력 (수정/배양/hCG 등) | **날짜 + 개수만** |
| 시작일~종료일 | **시작일만** (과배란, 이식대기) |

---

## UI 설계

### 기록 탭 메인 화면
```
┌─────────────────────────────────────────┐
│ 기록                                 🕐 │
│ 1차 시도                                │
├─────────────────────────────────────────┤
│ 📊 기록        📈 통계                  │
│ ━━━━━━━━                                │
├─────────────────────────────────────────┤
│                                         │
│ ┌─────────────────────────────────────┐ │
│ │ 🥚 1차 채취                  [편집] │ │
│ │    2025.12.07 ~                     │ │
│ │                                     │ │
│ │ ┌─────────────────────────────────┐ │ │
│ │ │ 현재 진행                       │ │ │
│ │ │ 이식 대기 중                    │ │ │
│ │ └─────────────────────────────────┘ │ │
│ │                                     │ │
│ │   채취       동결 잔여    이식 시도  │ │
│ │   6개         2개         0회       │ │
│ └─────────────────────────────────────┘ │
│                                         │
│ ─ ─ ─ ─ ─ 전체 단계 ─ ─ ─ ─ ─ ─ ─ ─ ─ │
│                                         │
│ ✅ 과배란         12.07 ~          ✏️   │
│ ✅ 채취           12.18    6개     ✏️   │
│ ▶️ 이식 대기      12.19 ~  2개     ✏️   │
│ ○ 이식            -                ✏️   │
│ ○ 판정            -                ✏️   │
│                                         │
├─────────────────────────────────────────┤
│ 🥚 새로운 채취 시작하기              >  │
├─────────────────────────────────────────┤
│           🕐 지난 기록 보기             │
└─────────────────────────────────────────┘
```

---

## 단계별 날짜 입력 방식

| 단계 | 날짜 타입 | 표시 예시 | 추가 입력 |
|------|-----------|-----------|-----------|
| 과배란 | 시작일만 | 12.07 ~ | - |
| 채취 | 당일 | 12.18 | 채취 개수 (선택) |
| 이식 대기 | 시작일만 | 12.19 ~ | 동결 개수 (선택) |
| 이식 | 당일 | 12.24 | - |
| 판정 | 당일 | 01.03 | 결과 (선택) |

---

## 단계 상태 표시

| 상태 | 아이콘 | 조건 |
|------|--------|------|
| 완료 | ✅ | 다음 단계 날짜가 입력됨 |
| 진행중 | ▶️ | 현재 단계 |
| 예정 | ○ | 날짜 미입력 |

---

## 바텀시트 편집 UI

### 과배란 편집
```
┌─────────────────────────────────────────┐
│                   ──                    │
│ 💉 과배란                               │
├─────────────────────────────────────────┤
│                                         │
│ 📅 시작일                               │
│ ┌─────────────────────────────────────┐ │
│ │ 2025.12.07                       📅 │ │
│ └─────────────────────────────────────┘ │
│                                         │
│ ┌─────────────────────────────────────┐ │
│ │              완료                   │ │
│ └─────────────────────────────────────┘ │
│                                         │
└─────────────────────────────────────────┘
```

### 채취 편집
```
┌─────────────────────────────────────────┐
│                   ──                    │
│ 🥚 채취                                 │
├─────────────────────────────────────────┤
│                                         │
│ 📅 날짜                                 │
│ ┌─────────────────────────────────────┐ │
│ │ 2025.12.18                       📅 │ │
│ └─────────────────────────────────────┘ │
│                                         │
│ 🥚 채취 개수 (선택)                     │
│ ┌─────────────────────────────────────┐ │
│ │        [-]      6개      [+]        │ │
│ └─────────────────────────────────────┘ │
│                                         │
│ ┌─────────────────────────────────────┐ │
│ │              완료                   │ │
│ └─────────────────────────────────────┘ │
│                                         │
└─────────────────────────────────────────┘
```

### 이식 대기 편집
```
┌─────────────────────────────────────────┐
│                   ──                    │
│ ⏳ 이식 대기                            │
├─────────────────────────────────────────┤
│                                         │
│ 📅 시작일                               │
│ ┌─────────────────────────────────────┐ │
│ │ 2025.12.19                       📅 │ │
│ └─────────────────────────────────────┘ │
│                                         │
│ ❄️ 동결 개수 (선택)                     │
│ ┌─────────────────────────────────────┐ │
│ │        [-]      2개      [+]        │ │
│ └─────────────────────────────────────┘ │
│                                         │
│ ┌─────────────────────────────────────┐ │
│ │              완료                   │ │
│ └─────────────────────────────────────┘ │
│                                         │
└─────────────────────────────────────────┘
```

### 이식 편집
```
┌─────────────────────────────────────────┐
│                   ──                    │
│ 🎯 이식                                 │
├─────────────────────────────────────────┤
│                                         │
│ 📅 날짜                                 │
│ ┌─────────────────────────────────────┐ │
│ │ 2025.12.24                       📅 │ │
│ └─────────────────────────────────────┘ │
│                                         │
│ ┌─────────────────────────────────────┐ │
│ │              완료                   │ │
│ └─────────────────────────────────────┘ │
│                                         │
└─────────────────────────────────────────┘
```

### 판정 편집
```
┌─────────────────────────────────────────┐
│                   ──                    │
│ 🩺 판정                                 │
├─────────────────────────────────────────┤
│                                         │
│ 📅 날짜                                 │
│ ┌─────────────────────────────────────┐ │
│ │ 2025.01.03                       📅 │ │
│ └─────────────────────────────────────┘ │
│                                         │
│ 🎉 결과 (선택)                          │
│ ┌───────────┬───────────┬───────────┐  │
│ │   성공    │   실패    │ 아직 모름  │  │
│ │     ○    │     ○    │     ●     │  │
│ └───────────┴───────────┴───────────┘  │
│                                         │
│ ┌─────────────────────────────────────┐ │
│ │              완료                   │ │
│ └─────────────────────────────────────┘ │
│                                         │
└─────────────────────────────────────────┘
```

---

## 데이터 모델

```dart
class TreatmentCycle {
  int cycleNumber;           // 채취 회차 (1차, 2차...)
  int attemptNumber;         // 시도 회차 (1차 시도, 2차 시도...)
  DateTime startDate;        // 사이클 시작일
  List<TreatmentStage> stages;
}

class TreatmentStage {
  StageType type;
  DateTime? startDate;       // 시작일 (과배란, 이식대기)
  DateTime? date;            // 당일 (채취, 이식, 판정)
  int? count;                // 개수 (채취: 난자수, 이식대기: 동결수)
  ResultType? result;        // 결과 (판정 단계만)
}

enum StageType {
  stimulation,   // 과배란
  retrieval,     // 채취
  waiting,       // 이식 대기
  transfer,      // 이식
  result,        // 판정
}

enum ResultType {
  success,       // 성공
  failure,       // 실패
  unknown,       // 아직 모름
}
```

---

## 상태 자동 계산 로직

```dart
StageStatus getStageStatus(TreatmentStage stage, TreatmentCycle cycle) {
  // 1. 다음 단계에 날짜가 있으면 → 완료
  final nextStage = getNextStage(stage, cycle);
  if (nextStage != null && nextStage.hasDate) {
    return StageStatus.completed;
  }
  
  // 2. 현재 단계에 날짜가 있고, 오늘 이전이면 → 진행중
  if (stage.hasDate && stage.date <= today) {
    return StageStatus.inProgress;
  }
  
  // 3. 그 외 → 예정
  return StageStatus.pending;
}
```

---

## 전체 단계 리스트 컴포넌트

```dart
Widget buildStageList(TreatmentCycle cycle) {
  return Column(
    children: [
      // 과배란
      StageRow(
        icon: '💉',
        name: '과배란',
        status: getStatus(cycle.stimulation),
        dateText: cycle.stimulation.startDate != null 
          ? '${formatDate(cycle.stimulation.startDate)} ~' 
          : '-',
        onEdit: () => showStageEditSheet(StageType.stimulation),
      ),
      
      // 채취
      StageRow(
        icon: '🥚',
        name: '채취',
        status: getStatus(cycle.retrieval),
        dateText: formatDate(cycle.retrieval.date) ?? '-',
        countText: cycle.retrieval.count != null 
          ? '${cycle.retrieval.count}개' 
          : null,
        onEdit: () => showStageEditSheet(StageType.retrieval),
      ),
      
      // 이식 대기
      StageRow(
        icon: '⏳',
        name: '이식 대기',
        status: getStatus(cycle.waiting),
        dateText: cycle.waiting.startDate != null 
          ? '${formatDate(cycle.waiting.startDate)} ~' 
          : '-',
        countText: cycle.waiting.count != null 
          ? '${cycle.waiting.count}개' 
          : null,
        onEdit: () => showStageEditSheet(StageType.waiting),
      ),
      
      // 이식
      StageRow(
        icon: '🎯',
        name: '이식',
        status: getStatus(cycle.transfer),
        dateText: formatDate(cycle.transfer.date) ?? '-',
        onEdit: () => showStageEditSheet(StageType.transfer),
      ),
      
      // 판정
      StageRow(
        icon: '🩺',
        name: '판정',
        status: getStatus(cycle.result),
        dateText: formatDate(cycle.result.date) ?? '-',
        onEdit: () => showStageEditSheet(StageType.result),
      ),
    ],
  );
}
```

---

## 요약

| 항목 | 내용 |
|------|------|
| 기본값 | 1차 채취 (0차 아님) |
| 전체 단계 | 항상 5개 모두 표시 |
| 날짜 입력 | 과배란/이식대기: 시작일만, 나머지: 당일 |
| 추가 입력 | 채취 개수, 동결 개수, 판정 결과 (모두 선택) |
| 편집 방식 | ✏️ 탭 → 바텀시트 |
| 상태 표시 | ✅완료 / ▶️진행중 / ○예정 |
